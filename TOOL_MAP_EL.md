# ETL Studio — ο χάρτης του εργαλείου

Συνολική εικόνα: τι είναι, από τι αποτελείται, πώς ρέουν τα δεδομένα, πού
αποθηκεύεται τι. Γραμμένο για να διαβαστεί με τη σειρά.

Τα νούμερα είναι μετρημένα, όχι κατ' εκτίμηση.

---

## Σε τρεις γραμμές

Τοπικό εργαλείο που παίρνει δεδομένα από **CSV, πίνακα SQL Server ή εγκεκριμένο
SELECT**, τα περνάει από κανόνες ανά στήλη, και γράφει **CSV ή XLSX** — μαζί με
χωριστό αρχείο για ό,τι απορρίφθηκε και τον λόγο.

Τρέχει μόνο στον υπολογιστή σου (loopback), χωρίς login, χωρίς cloud.

Python 3.12, χωρίς framework. Εξαρτήσεις: `openpyxl`, `lxml`, `pyodbc`,
`sqlglot`.

---

## 1. Η αλυσίδα, από άκρη σε άκρη

```
ΠΗΓΗ            ΜΗΧΑΝΗ                                    ΕΞΟΔΟΣ
────            ──────                                    ──────
CSV      ─┐                                            ┌─ valid.csv / .xlsx
SQL table ├─►  ανά πεδίο, με αυτή τη σειρά:             │
SELECT   ─┘    1 transforms                            └─ rejected.csv
                2 required                                 (γραμμή + στάδιο + λόγος)
                3 max_length
                4 τύπος
                5 lookup
```

**Η σειρά δεν είναι διακοσμητική.** Τα στάδια 1–3 βλέπουν την τιμή ως **κείμενο**.
Ο τύπος εφαρμόζεται μόνο μετά. Γι' αυτό ένα πεδίο μπορεί να περάσει το `required`
και να κοπεί στη μετατροπή — το είδαμε ζωντανά στο `examples/templates_demo/erp_c.csv`.

**Καμία γραμμή δεν χάνεται σιωπηλά.** Ό,τι απορρίπτεται γράφεται με την αρχική του
μορφή, τι είχε γίνει μέχρι εκεί, και ποιο στάδιο το έκοψε.

---

## 2. Τα τρία μοντέλα δεδομένων

Αυτό είναι το κλειδί για να καταλάβεις τον κώδικα. Τρία χωριστά πράγματα, επίτηδες:

| Μοντέλο | Απαντά στο ερώτημα |
|---|---|
| `SourceRow` | **τι διαβάστηκε** από την πηγή |
| `RowResult` | **τι συνέβη** — original, transformed, converted, και τα σφάλματα ανά πεδίο |
| Exporter | **πώς αναπαρίσταται** στο τελικό αρχείο |

Δεν συγχωνεύονται γιατί το ίδιο `RowResult` τροφοδοτεί ταυτόχρονα το αρχείο
εξόδου, το αρχείο απορρίψεων και τη διαγνωστική οθόνη — που θέλουν **διαφορετική**
αναπαράσταση της ίδιας γραμμής.

Όλα immutable (`MappingProxyType` + αμυντική αντιγραφή). Ο λόγος: ένα
`RowResult` περνάει από τρεις καταναλωτές· αν κάποιος το πείραζε, οι άλλοι δύο θα
έβγαζαν λάθος αποτέλεσμα χωρίς να σκάσει τίποτα.

---

## 3. Processing version — v1 vs v2

Κάθε pipeline δηλώνει ρητά `version`. Δεν υπάρχει σιωπηλό default.

| | v1 (legacy) | v2 (τρέχον) |
|---|---|---|
| τιμή που ξεκινά με `- + = @` | μπαίνει απόστροφος για Excel | **μένει ως έχει** |
| NULL vs κενό | το ίδιο πράγμα | **διαφορετικά** (`\N` vs κενό) |
| Decimal | μπορεί να περάσει από float | **ποτέ** |
| transforms | ανεκτικά | αυστηρά |

Τα καινούργια pipelines είναι **v2**. Το v1 υποστηρίζεται πλήρως και αμετάβλητο —
ό,τι έχει αποθηκευτεί κρατάει τη δική του έκδοση.

> **Ο κανόνας που κοστίζει αν τον αγνοήσεις:** ο target type είναι **επικύρωση,
> όχι μορφοποίηση**. Το `string` με v2 είναι lossless για κάθε τύπο ODBC που
> δοκιμάστηκε. Βάλε `decimal` μόνο όταν **θέλεις** να απορρίπτεται ό,τι δεν είναι
> αριθμός.

---

## 4. Τα modules (22 αρχεία, 3.162 γραμμές)

### Πυρήνας — τι είναι μια γραμμή

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `models.py` | 192 | τα immutable domain values· τίποτα για πηγή, UI, αποθήκευση ή έξοδο |
| `spec.py` | 98 | το συμβόλαιο του pipeline — το μοιράζονται CLI, UI και μηχανή |
| `serialization.py` | 160 | μετατροπή ορισμού και κωδικοποίηση διαγνωστικών στο όριο του JSON |

### Επεξεργασία

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `engine.py` | 178 | mapping και επικύρωση, με φραγμένα previews και streamed εξαγωγή |
| `transforms.py` | 28 | **μόνο** ρητά transforms· τη σειρά τη δίνει ο καλών |
| `validations.py` | 21 | έλεγχοι που διατηρούν την τιμή· η σειρά ανήκει στη μηχανή |
| `conversions.py` | 63 | μετατροπή τύπου· καμία επικύρωση, καμία μορφοποίηση διαγνωστικών |

Το ότι είναι **τέσσερα** αρχεία και όχι ένα είναι η ουσία: κανένα δεν ξέρει τη
σειρά των άλλων. Τη σειρά την ορίζει μόνο η `engine.py`.

### Είσοδος και έξοδος

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `sources.py` | 227 | context-managed αντάπτορες πηγής |
| `exporters.py` | 170 | όρια streaming εξόδου· ποτέ δεν μετασχηματίζει ούτε συμπεραίνει |
| `discovery.py` | 414 | read-only περιήγηση πριν την εκτέλεση — schemas, πίνακες, δείγμα |

### SQL Server

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `queries.py` | 229 | codec ερωτημάτων και **fail-closed** πολιτική ασφαλείας |
| `query_source.py` | 107 | μόνο εγκεκριμένο SELECT, bound παράμετροι |
| `query_discovery.py` | 69 | authored query datasets, χωριστά από τον κατάλογο πινάκων |

### Ενορχήστρωση και μνήμη

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `ordered.py` | 102 | codec για διαδοχικές ανεξάρτητες εξαγωγές |
| `ordered_runs.py` | 164 | σειριακός συντονιστής γύρω από **αμετάβλητη** μηχανή |
| `store.py` | 79 | SQLite: ορισμοί και στιγμιότυπα εκτελέσεων |
| `templates.py` | 279 | source-independent blueprints, immutable revisions, ρητό binding |
| `diagnostics.py` | 168 | παρουσίαση υπαρχόντων στοιχείων· **δεν ξαναδιαβάζει** την πηγή |

### Περίβλημα

| Αρχείο | Γραμμές | Ρόλος |
|---|---|---|
| `web.py` | 267 | loopback-only UI για έναν έμπιστο τοπικό χρήστη |
| `config.py` | 63 | τοπική εκκίνηση· **ποτέ** log σε connection strings ή περιεχόμενα |
| `__main__.py` | 83 | CLI: `serve`, `preview`, `run` |

---

## 5. Το frontend (8 αρχεία, 2.276 γραμμές, χωρίς build step)

| Αρχείο | Γραμμές | Τι κρατάει |
|---|---|---|
| `style.css` | 1.336 | 24 τιτλοφορημένες ενότητες. **Δεν ταξινομείται και δεν ξεδιπλώνεται** — έξι selectors ορίζονται δύο φορές επίτηδες, το δεύτερο είναι override |
| `app.js` | 407 | ο πυρήνας του editor, `action()`, render, notices |
| `controls.js` | 172 | Select2 τοπικά· τα κρυφά native inputs μένουν το όριο των τιμών |
| `templates.js` | 123 | ο πίνακας templates και το review των mappings |
| `index.html` | 107 | όλη η δομή |
| `ordered.js` · `query.js` · `diagnostics.js` | 131 | οι τρεις ειδικές οθόνες |

### Οι ενότητες της οθόνης, με τη σειρά

```
01 Discover a source     ── πηγή, σύνδεση, περιήγηση καταλόγου
   Reusable mapping templates
   Review mappings       ── εμφανίζεται μόνο μετά από Apply
02 Map & validate        ── μία γραμμή ανά πεδίο εξόδου
      Choose source columns
      Bulk transforms
03 Export                ── μορφή, διαχωριστικό, κωδικοποίηση
   Advanced              ── το JSON του pipeline
   Results / Run history
   Ordered query exports
```

---

## 6. Η επιφάνεια HTTP — 13 endpoints

Όλα POST με token, όλα loopback.

| | |
|---|---|
| `/api/bootstrap` | το token και η αρχική κατάσταση |
| `/api/columns` | ονόματα στηλών της πηγής |
| `/api/discovery/…` | namespaces, datasets, δείγμα |
| `/api/validate` · `/api/preview` | έλεγχος και 100 γραμμές |
| `/api/pipelines` · `/api/pipelines/delete` | αποθήκευση και διαγραφή |
| `/api/runs` | εκτέλεση |
| `/api/diagnostics/rejections` | οι απορρίψεις μιας εκτέλεσης |
| `/api/query/connections` · `/api/query/validate` | εγκεκριμένες συνδέσεις, έλεγχος SQL |
| `/api/ordered/preview` | preview ενός βήματος |
| `/api/templates/…` | `list`, `read`, `create`, `revision`, `apply`, `bind`, `match` |

---

## 7. Ασφάλεια SQL — τρία ανεξάρτητα στρώματα

1. **Deployment approval.** Το `ETL_QUERY_CONNECTIONS` απαριθμεί ρητά ποιες
   συνδέσεις και ποια `[schema, table]` επιτρέπονται. Αν λείπει, **τίποτα** δεν
   τρέχει — fail-closed.
2. **AST allowlist.** Το `sqlglot` παράγει δέντρο· επιτρέπονται συγκεκριμένες
   κλάσεις κόμβων **και** συγκεκριμένα γεμάτα ορίσματα. Ένα μόνο statement. Χωρίς
   `EXEC`, `INTO`, προσωρινούς πίνακες.
3. **Δικαιώματα βάσης.** Το πραγματικό όριο. Ο parser είναι άμυνα σε βάθος, όχι
   υποκατάστατο.

Επιτρεπόμενο υποσύνολο: `SELECT`, `WHERE`, `JOIN`, `GROUP BY`, `CAST`, `CONVERT`,
`TRIM`/`LTRIM`/`RTRIM`, συναθροίσεις.

---

## 8. Πού αποθηκεύεται τι

```
C:\etltool\
├── data\
│   ├── etl.sqlite3              δύο πίνακες: pipelines, runs
│   ├── templates\<id>\<rev>.json   ένα αρχείο ανά revision, ποτέ overwrite
│   └── runs\<run_id>\           valid.csv | .xlsx  +  rejected.csv
├── examples\                    το demo και το templates_demo
└── etl\                         ο κώδικας
```

Το `data\` είναι **εκτός git** (`.gitignore`).

Οι revisions γράφονται με προσωρινό αρχείο, `fsync`, και μετά **hard link** στο
τελικό όνομα. Αν το όνομα υπάρχει, η εγγραφή αποτυγχάνει — δεν πατάει ποτέ
προηγούμενη έκδοση, ούτε από δεύτερη διεργασία.

---

## 9. Έλεγχοι

**276 Python tests** σε 20 αρχεία:

| | |
|---|---|
| `test_source_contract.py` | 39 — το συμβόλαιο κάθε πηγής |
| `test_discovery.py` · `test_queries.py` | 50 — περιήγηση και ασφάλεια SQL |
| `test_models.py` · `test_exporters.py` | 39 — αμεταβλητότητα και όρια εξόδου |
| `test_ordered.py` · `test_legacy_semantics.py` | 36 |
| `test_desired_v2_semantics.py` | 16 — τι ακριβώς αλλάζει το v2 |
| `test_templates.py` | 15 |
| τα υπόλοιπα | 81 |

**12 browser tests** (Playwright, `.cjs`) που χτυπάνε δύο fixture servers σε
`8768` και `8769`.

> **Προσοχή στα Windows:** λόγω `SO_REUSEADDR`, δύο servers μπορούν να δεσμεύσουν
> την **ίδια** θύρα ταυτόχρονα, και τα αιτήματα πάνε στον **πρώτο**. Ένας
> ξεχασμένος server με παλιό state κάνει τα tests να αποτυγχάνουν με νούμερα που
> δεν βγάζουν νόημα (π.χ. «3 runs αντί 1»). Σταμάτα τους και ξεκίνα καθαρά.

---

## 10. Απόδοση — τι μετρήθηκε

```
διάβασμα CSV      13%
επεξεργασία       65%
γράψιμο CSV       22%
```

Και:

```
χωρίς κανένα transform        6,29s
trim σε ΟΛΕΣ τις 173 στήλες   6,56s    ← διαφορά 4%
```

**Τι σημαίνει:** το κόστος είναι **ανά τιμή**, όχι ανά transform. Το να σπρώξεις
τα `trim`/`upper` στην SQL αγοράζει ~4%. Ο πραγματικός μοχλός είναι να **περνάνε
λιγότερα δεδομένα** στην Python — λιγότερες στήλες, `WHERE`, `GROUP BY`.

Καταμερισμός: **SQL Server** για φιλτράρισμα, joins, συναθροίσεις. **Python** για
transforms, επικύρωση, μετατροπή τύπων, διαγνωστικά, αρχεία.

---

## 11. Τι είναι σε εξέλιξη

**Matching των templates** — μισοτελειωμένο, δηλωμένο. Υπάρχουν ήδη:
`propose_bindings`, `match_template`, το `match` endpoint, τα νέα κείμενα του UI
και οι handlers.

Λείπουν: **τα τεστ** (1–15 Python, 21–25 browser) και το CSS των καταστάσεων.
Μέχρι να γραφτούν, δεν έχει αποδειχθεί τίποτα.

Σχέδιο: [TEMPLATE_MATCHING_PLAN.md](TEMPLATE_MATCHING_PLAN.md).

---

## 12. Τα υπόλοιπα κείμενα

| Αρχείο | Σε ποιον απαντά |
|---|---|
| `README.md` | εγκατάσταση και πρώτη εκτέλεση |
| `USER_GUIDE_EL.md` | οδηγός χρήσης |
| `TEMPLATES_EXPLAINED_EL.md` | τα templates σε απλά λόγια, με παραδείγματα που τρέχουν |
| `REUSABLE_MAPPING_TEMPLATES.md` | η τεχνική προδιαγραφή των templates |
| `ARCHITECTURE_STAGES_1_2.md` … `_5.md` | η αρχιτεκτονική ανά στάδιο |
| `DEVELOPER_NOTE_DATA_FLOW.md` | η ροή δεδομένων για προγραμματιστή |
| `FRONTEND_WORKFLOW.md` | πώς δουλεύει το UI |
| `READ_ONLY_QUERY_SOURCES.md` | η πολιτική ασφαλείας των ερωτημάτων |
| `PROCESSING_DIAGNOSTICS.md` | τα διαγνωστικά απορρίψεων |
| `GUIDED_SOURCE_DISCOVERY.md` | η περιήγηση πηγών |
| `ORDERED_QUERY_PIPELINES.md` | οι διαδοχικές εξαγωγές |
| `TOOL_OVERVIEW_AND_DECISIONS_EL.md` | αποφάσεις και το σκεπτικό τους |
| `REVIEW_BRIEF.md` · `IMPLEMENTATION_PLAN.md` · `REVIEW_IMPLEMENTATION_RESULTS.md` | ο κύκλος της εξωτερικής αξιολόγησης |
