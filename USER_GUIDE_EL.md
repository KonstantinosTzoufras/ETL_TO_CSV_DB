# ETL Studio — Οδηγός της οθόνης

Η βασική σειρά εργασίας είναι:

**Επιλογή πηγής → αντιστοίχιση πεδίων και κανόνες → προεπισκόπηση → εξαγωγή.**

Μια **ροή (pipeline)** είναι το σύνολο αυτών των ρυθμίσεων. Για παράδειγμα: «Διάβασε τους πελάτες από CSV, καθάρισε τα ονόματα, έλεγξε τις ημερομηνίες και δημιούργησε Excel».

## Πάνω μέρος — όνομα και ενέργειες

| Επιλογή | Τι κάνει |
|---|---|
| **Pipeline name** | Δίνεις όνομα στη ροή, π.χ. «Εξαγωγή πελατών». |
| **Save pipeline** | Αποθηκεύει τις ρυθμίσεις για επόμενη χρήση. Δεν εκτελεί την εξαγωγή. |
| **Preview 100 rows** | Δοκιμάζει τη ροή στις πρώτες 100 εγγραφές εισόδου, χωρίς να δημιουργεί αρχεία εξαγωγής. |
| **Run & export** | Επεξεργάζεται όλες τις εγγραφές και δημιουργεί τα αρχεία αποτελέσματος. |

Η προεπισκόπηση και η εκτέλεση χρησιμοποιούν τις **τρέχουσες ρυθμίσεις της φόρμας**, ακόμη και αν δεν έχεις πατήσει Save.

## Αριστερό μενού — αποθηκευμένες ροές

- **New pipeline:** ξεκινά καινούρια, κενή ροή.
- **Your pipelines:** εμφανίζει όσες ροές έχεις αποθηκεύσει. Πατώντας μία, φορτώνεις τις ρυθμίσεις της.

Η αποθήκευση μιας ροής κρατά τις ρυθμίσεις της, όχι αντίγραφο των δεδομένων της πηγής. Σε νέα εκτέλεση διαβάζονται ξανά τα δεδομένα.

## 01 · Data source — από πού παίρνεις δεδομένα

Στο **Source type** επιλέγεις CSV ή SQL Server.

### Πηγή CSV

| Πεδίο | Τι συμπληρώνεις |
|---|---|
| **File path inside workspace** | Διαδρομή αρχείου μέσα στο project, π.χ. `examples/customers.csv`. Δεν είναι πεδίο ανεβάσματος αρχείου. |
| **Delimiter** | Τον χαρακτήρα που χωρίζει τις στήλες: `;`, κόμμα, tab ή pipe. |
| **Encoding** | Την κωδικοποίηση του αρχείου. Η προεπιλογή είναι `utf-8-sig`. |

Το **Read source columns** διαβάζει τις επικεφαλίδες και τις εμφανίζει κάτω από την πηγή. Αν η ροή δεν έχει πεδία εξόδου, δημιουργεί αρχικές αντιστοιχίσεις. Αν υπάρχουν ήδη αντιστοιχίσεις, τις διατηρεί.

### Πηγή SQL Server

| Πεδίο | Τι συμπληρώνεις |
|---|---|
| **Connection variable** | Το όνομα της ρύθμισης σύνδεσης, π.χ. `ETL_SQL_MAIN`. Δεν γράφεις εδώ κωδικό πρόσβασης. |
| **Schema** | Το schema του πίνακα, συνήθως `dbo`. |
| **Table** | Το όνομα του πίνακα ή view που θα διαβαστεί. |

Η σύνδεση πρέπει να έχει ρυθμιστεί πριν ξεκινήσει η εφαρμογή. Οι οδηγίες βρίσκονται στο [README](README.md#σύνδεση-sql-server).

## 02 · Map & validate — τι κρατάς και τι ελέγχεις

**Κάθε γραμμή αυτού του πίνακα ορίζει μία στήλη του τελικού αρχείου.** Δεν αντιστοιχεί σε μία εγγραφή πελάτη.

| Επιλογή | Τι κάνει |
|---|---|
| **Output name** | Το όνομα της στήλης στην εξαγωγή. Πρέπει να είναι μοναδικό. |
| **Value from** | `Column`: παίρνει τιμή από πεδίο της πηγής. `Constant`: βάζει την ίδια σταθερή τιμή σε κάθε εγγραφή. |
| **Source / Constant** | Το όνομα του πεδίου προέλευσης ή τη σταθερή τιμή. |
| **Type** | Ο τύπος στον οποίο θα μετατραπεί και θα ελεγχθεί η τιμή. |
| **Transforms** | Επεξεργασίες που εφαρμόζονται πριν από τη μετατροπή τύπου. |
| **Required** | Η τιμή δεν επιτρέπεται να είναι null, κενή ή μόνο κενά. |
| **Max length** | Μέγιστο μήκος μετά τους μετασχηματισμούς και πριν από τη μετατροπή τύπου. |
| **×** | Αφαιρεί τη στήλη από την εξαγωγή. Δεν αλλάζει την πηγή. |
| **Add field** | Προσθέτει νέα στήλη εξόδου. |

### Παράδειγμα αντιστοίχισης

```text
Πηγή: name = "  Maria Papadopoulou  "
Transform: trim
Output name: full_name

Αποτέλεσμα: full_name = "Maria Papadopoulou"
```

Παράδειγμα σταθερής τιμής: `Output name = origin`, `Value from = Constant`, `Source / Constant = CRM`. Κάθε εγγραφή θα έχει `origin = CRM`.

### Διαθέσιμοι μετασχηματισμοί

| Transform | Αποτέλεσμα |
|---|---|
| `trim` | Αφαιρεί κενά στην αρχή και στο τέλος. |
| `lower` | Μετατρέπει γράμματα σε πεζά. |
| `upper` | Μετατρέπει γράμματα σε κεφαλαία. |
| `empty_to_null` | Μετατρέπει το κενό κείμενο σε null, δηλαδή απουσία τιμής. |

Μπορείς να γράψεις `trim, lower`. Εκτελούνται με τη σειρά που τα γράφεις.

### Διαθέσιμοι τύποι

| Type | Χρήση |
|---|---|
| `string` | Κείμενο, ονόματα και κωδικοί που πρέπει να διατηρούν αρχικά μηδενικά. |
| `int` | Ακέραιος 64-bit. Το `003` απορρίπτεται· για τέτοιους κωδικούς διάλεξε `string`. |
| `decimal` | Ακριβής δεκαδικός, με τελεία ως υποδιαστολή, π.χ. `125.50`. |
| `float` | Αριθμός κινητής υποδιαστολής. |
| `bool` | Δέχεται `0`, `1`, `true`, `false`. |
| `date` | Πραγματική ημερομηνία με μορφή `YYYY-MM-DD`. |
| `datetime` | Ημερομηνία και ώρα σε μορφή ISO, π.χ. `2025-04-12T14:30:00`. |

Οι προαιρετικές κενές τιμές γίνονται null. Αν απαιτείται τιμή, ενεργοποίησε και το **Required**.

### Τι συμβαίνει όταν αποτύχει ένας κανόνας

Αν αποτύχει έστω ένα πεδίο, **ολόκληρη η εγγραφή αποκλείεται από το αρχείο έγκυρων αποτελεσμάτων**. Καταγράφεται στο αρχείο απορρίψεων, με τους λόγους αποτυχίας ανά πεδίο.

Αντίθετα, λάθη στη δομή της πηγής, όπως ανύπαρκτη στήλη ή λάθος αριθμός πεδίων σε CSV, προκαλούν αποτυχία της εκτέλεσης.

## 03 · Export destination — πώς θα βγει το αποτέλεσμα

- **File format:** επιλέγεις CSV ή Excel/XLSX.
- **CSV export delimiter:** επιλέγεις τον διαχωριστή της εξαγωγής CSV. Μπορεί να διαφέρει από τον διαχωριστή της πηγής. Δεν επηρεάζει το XLSX.

Με το **Run & export** δημιουργούνται:

1. Το αρχείο με τις έγκυρες εγγραφές, σε CSV ή XLSX.
2. Το `rejected.csv`, με τις απορρίψεις και τους λόγους τους.

Κάθε εκτέλεση δημιουργεί ξεχωριστά αρχεία. Στην παρούσα έκδοση τα κελιά XLSX γράφονται ως κείμενο, ώστε να διατηρούνται κωδικοί και ακριβείς δεκαδικές τιμές.

## Preview results — έλεγχος πριν από την εξαγωγή

Εμφανίζεται όταν πατήσεις **Preview 100 rows**.

- **Sampled records:** πόσες εγγραφές εισόδου εξετάστηκαν.
- **Valid:** πόσες πέρασαν τους ελέγχους.
- **Rejected:** πόσες απορρίφθηκαν.
- Ο πίνακας δείχνει τις τιμές μετά την επεξεργασία και τους λόγους απόρριψης.

Οι μετρήσεις αφορούν **μόνο το δείγμα**, όχι ολόκληρη την πηγή. Το δείγμα περιλαμβάνει και τις απορρίψεις· δεν αναζητά τις πρώτες 100 έγκυρες εγγραφές. Για SQL Server δεν υπάρχει εγγυημένη σειρά χωρίς ταξινόμηση.

## Run history — ιστορικό και λήψεις

Εμφανίζει τις πλήρεις εκτελέσεις με ημερομηνία, κατάσταση και μετρήσεις:

| Κατάσταση | Σημασία |
|---|---|
| `queued` | Περιμένει να ξεκινήσει. |
| `running` | Επεξεργάζεται δεδομένα. |
| `completed` | Ολοκληρώθηκε. Μπορεί να περιέχει απορρίψεις. |
| `failed` | Η εκτέλεση απέτυχε. Εμφανίζεται μήνυμα σφάλματος. |
| `interrupted` | Η εφαρμογή σταμάτησε πριν τελειώσει η εκτέλεση. |

Μετά την ολοκλήρωση εμφανίζονται:

- **Download CSV / Download XLSX:** λήψη των έγκυρων εγγραφών.
- **Rejected rows:** λήψη του αρχείου απορρίψεων.
- **Refresh:** ανανέωση του ιστορικού. Όσο υπάρχουν ενεργές εκτελέσεις, η οθόνη ανανεώνεται και αυτόματα.

Το αρχείο απορρίψεων περιέχει αριθμό εγγραφής, αρχικά δεδομένα, μετασχηματισμένες τιμές και λόγους σφάλματος. Κάθε run κρατά αντίγραφο των ρυθμίσεών του, ώστε μια μεταγενέστερη αλλαγή στη ροή να μην αλλάζει το ιστορικό του.

## Advanced · Pipeline definition — προχωρημένες ρυθμίσεις

Εδώ βλέπεις τη ροή ως **JSON**, δηλαδή μια δομημένη περιγραφή των επιλογών της.

Μπορείς να αντιγράψεις ή να τροποποιήσεις την περιγραφή. Το **Apply definition** εφαρμόζει τις αλλαγές στη φόρμα. Μετά χρειάζεται **Save pipeline** για να τις αποθηκεύσεις.

Εδώ ρυθμίζονται και τα **lookups**: έλεγχοι ότι μια τιμή υπάρχει σε άλλη πηγή, π.χ. ότι ο κωδικός χώρας υπάρχει σε έναν πίνακα επιτρεπόμενων χωρών. Παράδειγμα υπάρχει στο [README](README.md#κανόνες-και-σημασιολογία).

Για μια απλή ροή δεν χρειάζεται να αλλάξεις αυτό το section.

## Πρώτη δοκιμή με το demo

1. Άνοιξε την εφαρμογή στο `http://127.0.0.1:8765`, αφού την ξεκινήσεις.
2. Η αρχική φόρμα φορτώνει το demo **Customers · clean export**.
3. Πάτα **Read source columns** για να δεις τα πεδία του CSV.
4. Πάτα **Preview 100 rows**.
5. Θα δεις **5 εγγραφές: 3 έγκυρες και 2 απορρίψεις**:
   - Μία εγγραφή έχει ID `003` που δεν περνά τον αυστηρό έλεγχο ακεραίου και ανύπαρκτη ημερομηνία `2025-02-30`.
   - Μία εγγραφή έχει κενό όνομα, ενώ το πεδίο είναι υποχρεωτικό.
6. Πάτα **Save pipeline** για να κρατήσεις τις ρυθμίσεις.
7. Διάλεξε CSV ή XLSX και πάτα **Run & export**.
8. Κατέβασε τα αποτελέσματα από το **Run history**.

Για εγκατάσταση, εκκίνηση και τεχνικούς περιορισμούς της πρώτης έκδοσης, δες το [README](README.md).
