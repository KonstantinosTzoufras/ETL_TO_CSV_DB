# Reusable mapping templates — τι είναι, πότε το θέλεις

Οδηγός σε απλά λόγια. Το τεχνικό αντίστοιχο είναι το
[REUSABLE_MAPPING_TEMPLATES.md](REUSABLE_MAPPING_TEMPLATES.md).

---

## Σε μία πρόταση

Ένα template είναι **η περιγραφή του αρχείου που θέλεις να βγει** — ονόματα
στηλών, τύποι, κανόνες — **χωρίς να λέει από πού έρχονται τα δεδομένα**.

Αυτό είναι όλο το νόημα. Το «από πού» το λες χωριστά, κάθε φορά.

---

## Το πρόβλημα που λύνει

Έστω ότι πρέπει να στέλνεις κάθε μήνα ένα αρχείο με αυτές τις στήλες:

| Στήλη εξόδου | Τύπος | Κανόνες |
|---|---|---|
| `customer_code` | string | υποχρεωτικό, max 10, trim |
| `amount` | decimal | — |
| `country` | string | uppercase |

Τα δεδομένα όμως έρχονται από **τρεις διαφορετικές βάσεις**, και καμία δεν
ονομάζει τις στήλες της έτσι. Η πρώτη τα λέει `KWDIKOS`, `POSO`, `XWRA`, η
δεύτερη `cust_id`, `total`, `cc`, η τρίτη αλλιώς.

**Χωρίς template:** στήνεις τρία pipelines στο χέρι. Πρέπει να θυμάσαι σε καθένα
ότι το `customer_code` θέλει max 10 και trim. Αν το ξεχάσεις σε ένα, τα τρία
αρχεία δεν είναι πια ίδια — και θα το καταλάβεις όταν σκάσει παρακάτω.

**Με template:** γράφεις τους κανόνες **μία φορά**. Μετά, για κάθε βάση, λες μόνο
ποια στήλη της αντιστοιχεί σε ποια στήλη εξόδου. Οι κανόνες έρχονται μόνοι τους
και είναι αναγκαστικά ίδιοι και στα τρία.

---

## Πότε **δεν** το θέλεις

**Αν βγάζεις έναν πίνακα όπως είναι.** Διάλεξε πηγή, `Add selected columns`,
τέλος. Το template εκεί σου προσθέτει μόνο δουλειά.

**Αν το κάνεις μία φορά.** Το κέρδος βγαίνει από την επανάληψη.

Το template αξίζει όταν ισχύει και το ένα και το άλλο: **πολλές διαφορετικές
πηγές** πρέπει να δώσουν **πανομοιότυπο αρχείο**.

---

## Τι κρατάει και τι δεν κρατάει

Ανά στήλη εξόδου κρατάει:

- το **όνομα** που θα βγει στο αρχείο
- τον **τύπο** (string, int, decimal, float, bool, date, datetime)
- τα **transforms** με τη σειρά τους (trim, upper, lower, empty_to_null)
- αν είναι **υποχρεωτικό**
- το **max length**
- αν **απαιτεί lookup**

Δεν κρατάει — και τα απορρίπτει ρητά αν προσπαθήσεις να τα βάλεις:

- από ποια στήλη της πηγής έρχεται
- ποια βάση, ποιο connection, ποια credentials
- σταθερές τιμές

Γι' αυτό το ίδιο template μπαίνει πάνω σε τρεις άσχετες βάσεις.

---

## Τα βήματα

### 1 — Φτιάξε το template (μία φορά στη ζωή του)

`Create or revise a template` → δώσε όνομα → πρόσθεσε ένα target field ανά στήλη
εξόδου → **Create new template**.

Από δω και πέρα ζει στον δίσκο. **Δεν το ξαναφτιάχνεις.**

### 2 — Ξεκίνα καθαρό pipeline

**＋ New pipeline** και διάλεξε την πηγή.

Πρέπει να είναι **άδειο**, χωρίς ούτε ένα mapping. Το template δεν προσθέτει σε
υπάρχοντα και δεν τα αντικαθιστά — αν έχεις έστω ένα, σου λέει όχι.

### 3 — Βάλε το template πάνω

`List templates` → διάλεξε revision → **Apply selected revision**.

### 4 — Δέσε κάθε στήλη

Ανοίγει το **Review mappings** με ένα μπλοκ ανά στήλη εξόδου. Σε καθένα λες από
πού έρχεται: μια στήλη της πηγής, ή σταθερή τιμή (κείμενο / NULL / κενό / true /
false). Όσα μένουν ανοιχτά είναι σημειωμένα, και το **Apply mappings** παραμένει
κλειδωμένο μέχρι να απαντηθούν όλα.

Δύο βοηθήματα, κανένα από τα οποία αποφασίζει μόνο του:

- **Load binding** — φορτώνει ένα δέσιμο που **αποθήκευσες παλιότερα για αυτή την
  πηγή**. Αυτό είναι το πραγματικό κέρδος: το `KOD_PEL → customer_code` το λες
  **μία φορά**.
- **Match remaining by name** — προτείνει μόνο όπου τα ονόματα **ταυτίζονται**
  (ίδιο, ή με άλλα πεζά/κεφαλαία ή σημεία στίξης). Δεν πλησιάζει ποτέ ένα
  `KOD_PEL`, και αν δύο στήλες ταιριάζουν εξίσου σταματάει και ρωτάει.

Στο τέλος: **Apply mappings**.

Βγαίνει ένα συνηθισμένο pipeline. Από κει και πέρα δουλεύει σαν κάθε άλλο —
preview, save, run.

---

## Ένα πλήρες παράδειγμα που τρέχει

Τα αρχεία είναι στο [examples/templates_demo/](examples/templates_demo/). Τα
αποτελέσματα παρακάτω είναι **πραγματικά** — βγήκαν τρέχοντας τον κώδικα.

### Το template: «Monthly customer extract»

| Στήλη εξόδου | Τύπος | Transforms | Required | Max length |
|---|---|---|---|---|
| `customer_code` | string | trim | ναι | 10 |
| `amount` | decimal | — | όχι | — |
| `country` | string | upper | όχι | — |
| `origin` | string | — | όχι | — |

Πουθενά δεν λέει από πού έρχονται. Μόνο τι πρέπει να βγει.

### Δύο πηγές που δεν μοιάζουν σε τίποτα

`erp_a.csv` — διαχωριστικό `;`, ελληνικά ονόματα στηλών, σκουπίδια γύρω από τιμές:

```
KWDIKOS;POSO;XWRA
  A-1001  ;1234.50;gr
A-1002;-99.00;GR
A-1003;0.00;cy
```

`erp_b.csv` — διαχωριστικό `,`, άλλα ονόματα, και μια στήλη παραπάνω που δεν τη
θέλουμε:

```
cust_id,total,cc,extra_noise
B7,15.75,it,ignore me
B8,-3.10,IT,ignore me
B9,0.00,fr,ignore me
```

### Το δέσιμο — το μόνο που δηλώνεις χωριστά

| Στήλη εξόδου | Από το A | Από το B |
|---|---|---|
| `customer_code` | `KWDIKOS` | `cust_id` |
| `amount` | `POSO` | `total` |
| `country` | `XWRA` | `cc` |
| `origin` | σταθερό `ERP-A` | σταθερό `ERP-B` |

Το `extra_noise` απλώς δεν το διαλέγεις. Δεν υπάρχει στο template, άρα δεν βγαίνει.

### Το αποτέλεσμα

```
customer_code;amount;country;origin        customer_code;amount;country;origin
A-1001;1234.50;GR;ERP-A                    B7;15.75;IT;ERP-B
A-1002;-99.00;GR;ERP-A                     B8;-3.10;IT;ERP-B
A-1003;0.00;CY;ERP-A                       B9;0.00;FR;ERP-B
```

Ίδια κεφαλίδα, ίδιοι τύποι, ίδιοι κανόνες. Πρόσεξε τι έγινε μόνο του:

- `  A-1001  ` → `A-1001` (το trim του template)
- `gr` → `GR`, `it` → `IT` (το upper)
- `-99.00` έμεινε **αρνητικός αριθμός**, χωρίς απόστροφο μπροστά — αυτό είναι το
  processing version 2

### Πόσο διαφέρουν τα δύο pipelines που βγήκαν

Έβγαλα diff στα δύο παραγόμενα αρχεία. Διαφέρουν **μόνο** σε αυτά:

```
 "name": "Monthly extract - erp_a"    |  "name": "Monthly extract - erp_b"
 "path": ".../erp_a.csv"              |  "path": ".../erp_b.csv"
 "delimiter": ";"                     |  "delimiter": ","
 "source": "KWDIKOS"                  |  "source": "cust_id"
 "source": "POSO"                     |  "source": "total"
 "source": "XWRA"                     |  "source": "cc"
 "literal": "ERP-A"                   |  "literal": "ERP-B"
```

Τύποι, transforms, required, max_length: **καμία διαφορά**. Αυτό ακριβώς αγοράζεις
με το template — δεν γίνεται να ξεχάσεις το `max_length: 10` στο δεύτερο.

---

## Τι γίνεται όταν τα δεδομένα δεν σέβονται τους κανόνες

Τρίτη πηγή, `erp_c.csv`, με το ίδιο ακριβώς template:

```
KWDIKOS;POSO;XWRA
C-1;10.00;es
C-THIS-CODE-IS-TOO-LONG;20.00;es
;30.00;es
C-4;not a number;es
```

Αποτέλεσμα: **processed 4, valid 1, invalid 3**.

Στο `valid.csv` περνάει μόνο η πρώτη. Οι άλλες τρεις πάνε στο `rejected.csv`, η
καθεμία με το ακριβές στάδιο που την έκοψε:

| Γραμμή | Στάδιο | Μήνυμα |
|---|---|---|
| `C-THIS-CODE-IS-TOO-LONG` | `max_length` | exceeds 10 characters |
| κενός κωδικός | `required` | required value is missing |
| `not a number` | `conversion` | expected a number using a decimal point |

Δύο πράγματα να προσέξεις:

**Καμία γραμμή δεν χάνεται σιωπηλά.** Το `rejected.csv` κρατάει και την αρχική
γραμμή όπως διαβάστηκε, και τι είχε γίνει μέχρι να σκάσει.

**Η σειρά των σταδίων μετράει.** Το `C-THIS-CODE-IS-TOO-LONG` κόπηκε στο
`max_length`, **πριν** καν δοκιμαστεί ο τύπος. Και το `not a number` πέρασε το
required (έχει τιμή), και κόπηκε μετά στη μετατροπή σε decimal.

---

## Ποιο παράδειγμα **δεν** χρειάζεται template

Μία βάση, ένας πίνακας, τον θέλεις όπως είναι:

```
Discover a source → διάλεξε πίνακα → Add selected columns → Run
```

Εδώ το template θα σε έβαζε να γράψεις τα ονόματα των στηλών δεύτερη φορά, στο
χέρι, για να τα δέσεις μετά πάλι στα ίδια. Καθαρή ζημιά.


---

## Αποθηκευμένα δεσίματα (saved bindings)

Το template λέει **τι** πρέπει να βγει. Το αποθηκευμένο δέσιμο λέει **από πού**,
για μία συγκεκριμένη πηγή.

Στην πράξη:

| Πηγή | Δέσιμο |
|---|---|
| `ERP_A.Customers` | `customer_code ← KOD_PEL`, `amount ← POSO`, `origin = "ERP-A"` |
| `ERP_B.Customers` | `customer_code ← CUSTNO`, `amount ← TOTAL`, `origin = "ERP-B"` |

Την πρώτη φορά τα δένεις στο χέρι και πατάς **Save as new binding**. Κάθε επόμενη
φορά: το διαλέγεις, **Load binding**, κοιτάς, **Apply mappings**.

### Τι γίνεται όταν κάτι αλλάξει

Σε κάθε φόρτωση ελέγχονται και το template και η πηγή:

| Κατάσταση | Τι γίνεται |
|---|---|
| η στήλη υπάρχει ακόμη | αποκαθίσταται |
| **έφυγε από την πηγή** | το δέσιμο πέφτει, το target ανοίγει, **με το όνομα της στήλης στο μήνυμα** |
| **νέο target** στο template | ανοιχτό |
| **target που αφαιρέθηκε** | αναφέρεται και πέφτει, ποτέ σιωπηλά |
| **άλλαξαν οι κανόνες** του target | το δέσιμο κρατιέται, αλλά σου το λέει |

Το τελευταίο θέλει προσοχή: το δέσιμο λέει *από πού*, ο κανόνας λέει *τι πρέπει
να ισχύει*. Αν το `amount` έγινε `decimal`, η ίδια στήλη μπορεί τώρα να αρχίσει να
κόβει γραμμές — γι' αυτό στο λέει.

### Οι σταθερές ανήκουν εδώ

Το `origin = "ERP-A"` είναι η ταυτότητα της πηγής. Γι' αυτό το **template το
απορρίπτει** και το **δέσιμο το κρατάει**.

### Πού αποθηκεύονται

Στη βάση `data/etl.sqlite3`, πίνακας `binding_profiles`. **Ποτέ connection string,
κωδικός ή host** — μόνο το *όνομα* της μεταβλητής περιβάλλοντος, που ήδη υπάρχει
σε κάθε αποθηκευμένο pipeline.

Η διαγραφή ενός δεσίματος **δεν πειράζει** κανένα pipeline: το καθένα κουβαλάει τα
δικά του.

### Μια γνωστή αδυναμία

Ένα CSV δέσιμο αναγνωρίζεται από τη **διαδρομή του αρχείου**. Αν το μετακινήσεις,
το δέσιμο δεν θα εμφανιστεί πια ως «για αυτή την πηγή» — θα είναι στη λίστα πιο
κάτω, και μπορείς να το φορτώσεις κανονικά. Καμία αυτόματη επιλογή δεν γίνεται
ποτέ, οπότε δεν κινδυνεύεις από λάθος δέσιμο.

---

## Τι είναι το revision

Μια **αποθηκευμένη έκδοση** του template.

Όταν αλλάζεις κάτι και πατάς `Save new revision`, φτιάχνεται η επόμενη έκδοση.
Η προηγούμενη **δεν σβήνεται και δεν πειράζεται ποτέ**. Στον δίσκο το γράψιμο
γίνεται με τρόπο που αρνείται να πατήσει υπάρχον αρχείο.

Γιατί έχει σημασία: τα pipelines που έφτιαξες τον περασμένο μήνα **δεν κρατούν
καμία σύνδεση** με το template. Είναι κανονικά, αυτοτελή pipelines. Αν αύριο
αλλάξεις το template, **τίποτα υπάρχον δεν αλλάζει**. Ούτε καλά ούτε κακά — απλώς
δεν τα αγγίζει.

Αν θέλεις να πάρουν τους νέους κανόνες, φτιάχνεις καινούργιο pipeline με τη νέα
revision.

---

## Πώς αντιγράφεις ένα υπάρχον template

Χρήσιμο όταν το καινούργιο μοιάζει πολύ με ένα που ήδη έχεις.

1. `List templates` → διάλεξέ το
2. **Load revision into editor**
3. **Άλλαξε το Template name** ← μην το ξεχάσεις
4. **Create new template**

Όλες οι στήλες μεταφέρονται με τύπους, transforms και κανόνες. Το πρωτότυπο μένει
άθικτο· το αντίγραφο ξεκινά από revision 1 με δικό του id.

Αν ξεχάσεις το βήμα 3, θα έχεις **δύο templates με το ίδιο όνομα**. Επιτρέπεται,
και στη λίστα θα ξεχωρίζουν μόνο από το revision.

---

## Πού σώζονται

```
C:\etltool\data\templates\<id>\<revision>.json
```

Απλά αρχεία JSON, ένα ανά revision. Μπορείς να τα ανοίξεις και να τα διαβάσεις.

**Δεν υπάρχει κουμπί διαγραφής.** Για να φύγει ένα template, σβήνεις τον φάκελό
του με το χέρι. Τα pipelines που έχουν ήδη φτιαχτεί από αυτό **δεν παθαίνουν
τίποτα** — δεν το χρειάζονται πια.

---

## Οι παγίδες

**«Templates apply only to drafts with no mappings»**
Το pipeline έχει ήδη στήλες. Ξεκίνα καθαρό με ＋ New pipeline.

**«Template and pipeline processing versions differ»**
Το template είναι v1 και το pipeline v2, ή ανάποδα. Δεν αλλάζει version από μόνο
του — θα ήταν σιωπηλή αλλαγή στο πώς γράφονται τα δεδομένα.

**«unmapped target (optional targets also need an explicit binding)»**
Το `required: false` σημαίνει «**η τιμή** επιτρέπεται να είναι κενή», όχι
«μπορείς να μην απαντήσεις». Κάθε στήλη θέλει ρητή απάντηση, ακόμα κι αν η
απάντηση είναι «άφησέ τη NULL».

**«Load a revision before saving a new revision»**
Το `Save new revision` χρειάζεται να ξέρει *ποιου* template είναι η νέα έκδοση.
Πάτα πρώτα `Load revision into editor`. Αν το προσπεράσεις και πατήσεις
`Create new template`, φτιάχνεις δεύτερο template αντί για revision — έτσι
γεμίζει η λίστα με διπλά.

**«A newer revision exists; load it before saving»**
Κάποιος (ή εσύ σε άλλη καρτέλα) αποθήκευσε στο μεταξύ. Ξαναφόρτωσε.

---

## Δύο πράγματα που δεν κάνει

**Δεν ταιριάζει ονόματα μόνο του.** Ακόμα κι αν η πηγή έχει στήλη με ακριβώς το
ίδιο όνομα, πρέπει να τη διαλέξεις. Είναι σκόπιμο: ένα λάθος αυτόματο ταίριασμα
περνάει απαρατήρητο και βγάζει λάθος δεδομένα.

**Δεν φτιάχνει template από υπάρχον pipeline.** Αν έχεις ήδη στήσει στήλες σε ένα
pipeline και θέλεις να γίνουν template, αυτή τη στιγμή πρέπει να τις ξαναγράψεις.
