# ETL Studio — πώς δουλεύει, και τι λείπει

Ενοποιημένος οδηγός σε απλά λόγια, γραμμένος στις 22/09/2026 — αντικαθιστά τα
παλιότερα `TOOL_MAP_EL.md`, `TOOL_OVERVIEW_AND_DECISIONS_EL.md` και
`USER_GUIDE_EL.md`, που είχαν σοβαρή επικάλυψη μεταξύ τους. Ό,τι νούμερο
βλέπεις είναι μετρημένο εκείνη τη στιγμή, όχι κατ' εκτίμηση.

---

## Σε δύο προτάσεις

Παίρνεις δεδομένα από **ένα CSV, έναν πίνακα SQL Server, ή ένα εγκεκριμένο
SELECT**, τα περνάς από κανόνες που ορίζεις εσύ ανά στήλη, και βγάζεις
**CSV, XLSX, ή γράφεις κατευθείαν σε πίνακα SQL Server** — μαζί με χωριστό
αρχείο που λέει ακριβώς ποιες γραμμές απορρίφθηκαν και γιατί.

Τρέχει μόνο στον υπολογιστή σου. Δεν στέλνει τίποτα πουθενά αλλού.

---

## 1. Η αλυσίδα, βήμα-βήμα

```
ΠΗΓΗ                    ΓΙΑ ΚΑΘΕ ΠΕΔΙΟ ΕΞΟΔΟΥ, ΜΕ ΑΥΤΗ ΤΗ ΣΕΙΡΑ         ΕΞΟΔΟΣ
────                    ─────────────────────────────────────         ──────
CSV       ─┐            1. transforms   (trim, upper, lower...)    ┌─ valid.csv/.xlsx ή πίνακας SQL
Πίνακας SQL├─►           2. required     (υποχρεωτικό;)             ├─ rejected.csv
SELECT    ─┘            3. max_length   (πολύ μεγάλο;)             │  (ποια γραμμή,
                         4. τύπος        (int/decimal/date/...)     │   ποιο στάδιο,
                         5. lookup       (υπάρχει στην άλλη λίστα;) └─  γιατί)
```

**Η σειρά έχει σημασία.** Τα βήματα 1-3 βλέπουν την τιμή σαν **κείμενο**. Ο
τύπος ελέγχεται μόνο στο βήμα 4. Γι' αυτό μια τιμή μπορεί να περάσει το
"υποχρεωτικό" (έχει κάτι μέσα) και να κοπεί μετά επειδή δεν είναι αριθμός.

**Καμία γραμμή δεν χάνεται σιωπηλά.** Ό,τι απορρίπτεται γράφεται στο
`rejected.csv` με την αρχική του μορφή, τι είχε γίνει μέχρι εκεί, και ποιο
ακριβώς στάδιο το έκοψε.

---

## 2. Τι θεωρείται "σωστό": v1 vs v2

Κάθε pipeline δηλώνει ρητά ποια έκδοση επεξεργασίας χρησιμοποιεί.

| | v1 (παλιό) | v2 (τρέχον, default) |
|---|---|---|
| τιμή που ξεκινά με `- + = @` | βάζει απόστροφο (προστασία Excel) | **μένει όπως είναι** |
| NULL vs κενό κείμενο | ίδιο πράγμα | **διαφορετικά πράγματα** |
| Decimal | μπορεί να χαθεί ακρίβεια | **ποτέ μέσω float** |

> **Ο κανόνας που πρέπει να θυμάσαι:** ο τύπος ενός πεδίου είναι **έλεγχος
> εγκυρότητας**, όχι μορφοποίηση. Αν βάλεις `decimal` σε στήλη που κατά
> 99,9% είναι αριθμός αλλά έχει και κάνα σκουπίδι, αυτό το σκουπίδι θα
> **απορριφθεί** — δεν θα προσπαθήσει να το "μαντέψει". Αν δεν είσαι
> σίγουρος, βάλε `string`: δεν χάνεται καμία πληροφορία, απλώς δεν κάνεις
> έλεγχο τύπου.

---

## 3. Τα τέσσερα πράγματα που μπορείς να φτιάξεις

### Απλό pipeline
Διαλέγεις πηγή → λες ποιες στήλες θες και πώς λέγονται στο τέλος → Run.
Αυτό είναι το 90% των περιπτώσεων.

### Template + Saved binding (για επαναλαμβανόμενη δουλειά)
Έστω ότι πρέπει να στέλνεις **το ίδιο ακριβώς αρχείο** από **πολλές
διαφορετικές βάσεις**, που η καθεμιά ονομάζει τις στήλες της αλλιώς
(`KOD_PEL` σε μία, `CUSTNO` σε άλλη, `IDCLIENT` σε τρίτη).

- Το **template** λέει τι πρέπει να βγει: ονόματα, τύποι, κανόνες. Ποτέ
  δεν λέει από πού έρχεται — αλλιώς δεν θα ήταν επαναχρησιμοποιήσιμο.
- Το **saved binding** είναι η απόφασή σου, αποθηκευμένη, ότι *"σε αυτή
  τη συγκεκριμένη βάση, το `customer_code` έρχεται από το `KOD_PEL`"*.
  Την πρώτη φορά τη δηλώνεις στο χέρι· κάθε επόμενη φορά τη φορτώνεις.
- Ένα κουμπί **Match remaining by name** προτείνει μόνο εκεί που τα
  ονόματα ταιριάζουν ήδη (ίδιο, ή διαφορά σε πεζά/κεφαλαία) — δεν
  μαντεύει ποτέ κάτι σαν `KOD_PEL → customer_code`, γιατί αυτό είναι
  ακριβώς η κρίση που μόνο εσύ μπορείς να κάνεις.
- **Generate fields from inspected table**: αν το template περιγράφει ό,τι
  ήδη έχει ένας πίνακας SQL Server, δεν χρειάζεται να γράψεις τα πεδία στο
  χέρι — αφού τον επιθεωρήσεις (Discover a source), το κουμπί προτείνει
  όνομα/τύπο/μήκος/required ανά στήλη από τον ίδιο τον κατάλογο της βάσης.
  Πρόταση, όχι απόφαση: κάθε γραμμή ελέγχεται πριν το Create.
- Τίποτα δεν εφαρμόζεται μέχρι να πατήσεις **Apply mappings**, και το
  κουμπί μένει κλειδωμένο όσο μένει έστω ένα πεδίο χωρίς απόφαση.

Πλήρες παράδειγμα με πραγματικά δεδομένα: [TEMPLATES_EXPLAINED_EL.md](TEMPLATES_EXPLAINED_EL.md).

### Ordered query exports (πολλά βήματα, μία σύνδεση)
Σειρά από ανεξάρτητα SELECT στην ίδια εγκεκριμένη σύνδεση, το καθένα με
δικό του αρχείο εξόδου. Χρήσιμο όταν θες πολλά "κομμάτια" από την ίδια
βάση σε ένα πάτημα.

### DB→DB export (γράψιμο κατευθείαν σε πίνακα)
Αντί για αρχείο, ο προορισμός μπορεί να είναι ένας πίνακας σε εγκεκριμένη
SQL Server σύνδεση (χωριστή έγκριση από την ανάγνωση — `ETL_EXPORT_CONNECTIONS`,
ποτέ η ίδια με `ETL_QUERY_CONNECTIONS`). Ο πίνακας παίρνει το όνομά του από το
pipeline (πρόθεμα `z0_`), εκτός αν ορίσεις ρητά δικό σου όνομα στο πεδίο "Table
name" — και στις δύο περιπτώσεις δεσμεύεται μόνιμα σε αυτό το pipeline (άλλο pipeline
δεν μπορεί να κλέψει το ίδιο όνομα), και κάθε run γράφει πρώτα σε ένα "σκιώδη"
πίνακα — μόνο αν ολοκληρωθεί χωρίς σφάλμα μετονομάζεται στη θέση του παλιού,
ατομικά. Ένα run που αποτυγχάνει, οποτεδήποτε, δεν αφήνει ποτέ μισοτελειωμένο
πίνακα και δεν πειράζει τον προηγούμενο επιτυχημένο.

---

## 4. Το SQL — τρία ανεξάρτητα στρώματα ασφάλειας

1. **Έγκριση σύνδεσης** (`ETL_QUERY_CONNECTIONS` για ανάγνωση,
   `ETL_EXPORT_CONNECTIONS` για γράψιμο σε πίνακα — δύο ξεχωριστές, ποτέ η
   μία δεν υποκαθιστά την άλλη). Χωρίς αυτό, τίποτα δεν τρέχει. Η ανάγνωση
   μπορεί να εγκρίνει συγκεκριμένους πίνακες ή ολόκληρο schema (`["dbo", "*"]`).
2. **Parser που επιτρέπει μόνο SELECT** (μόνο για ελεύθερη SQL, όχι για τους
   κανονικούς πίνακες). Ένα statement, μόνο ανάγνωση.
   `INSERT/UPDATE/DELETE/DROP/EXEC/INTO` απορρίπτονται πάντα, όσο κι αν
   εγκρίνεις τη σύνδεση.
3. **Τα δικαιώματα του χρήστη της βάσης.** Η πραγματική άμυνα. Το parser
   είναι πρόσθετη ασφάλεια, όχι υποκατάστατο.

Σημείωση: **οι κανονικοί πίνακες** (Discover a source) δεν περνάνε από
τον parser καθόλου — διαβάζονται ελεύθερα, μέσω `ETL_QUERY_CONNECTIONS`. Το
DB export γράφει μόνο στο σχήμα/πίνακα που εγκρίνει ρητά το
`ETL_EXPORT_CONNECTIONS`.

---

## 5. Πού σώζεται τι

```
C:\etltool\
├── .env                          credentials βάσης + ETL_QUERY_CONNECTIONS + ETL_EXPORT_CONNECTIONS
├── data\
│   ├── etl.sqlite3                pipelines, runs, saved bindings, pipeline_exports (table claims)
│   ├── templates\<id>\<rev>.json   ένα αρχείο ανά revision, ποτέ δεν σβήνεται
│   └── runs\<run_id>\              valid.csv/.xlsx + rejected.csv (ή μόνο rejected.csv για DB export)
```

**Templates**: αρχεία, immutable — κάθε revision μένει για πάντα, ό,τι
έφτιαξες παλιότερα δεν χαλάει ποτέ.
**Saved bindings**: γραμμές σε βάση, mutable — μπορείς να τις ενημερώσεις
ή να τις σβήσεις, χωρίς να πειράξεις pipelines που ήδη τις χρησιμοποίησαν.

---

## 6. Ό,τι αποθηκεύεις, δεν το αγγίζει τίποτα άλλο

Ένα pipeline που αποθηκεύεις είναι **αυτοτελές**. Αν αύριο αλλάξεις το
template ή διαγράψεις το saved binding από το οποίο γεννήθηκε, το ίδιο το
pipeline **δεν επηρεάζεται καθόλου** — απλά κρατάει μια σημείωση (όχι
δεσμευτική) που λέει από πού βγήκε, για να θυμάσαι.

---

## 7. Τι δεν αποθηκεύει τίποτα μόνο του

Επιβεβαιωμένο, όχι απλώς δηλωμένο: **τίποτα δεν γράφεται στον δίσκο ή στη
βάση χωρίς να πατήσεις εσύ ένα συγκεκριμένο κουμπί.** Το Preview δεν
γράφει αρχεία. Ούτε το Inspect. Το Save pipeline λέει ρητά αν θα
δημιουργήσει καινούργιο ή θα ενημερώσει το υπάρχον, και ρωτάει πριν
φτιάξει διπλότυπο.

---

## 8. Τα modules (24 αρχεία Python, 4.042 γραμμές πυρήνα)

### Πυρήνας — τι είναι μια γραμμή

| Αρχείο | Ρόλος |
|---|---|
| `models.py` | τα immutable domain values· τίποτα για πηγή, UI, αποθήκευση ή έξοδο |
| `spec.py` | το συμβόλαιο του pipeline — το μοιράζονται CLI, UI και μηχανή |
| `serialization.py` | μετατροπή ορισμού και κωδικοποίηση διαγνωστικών στο όριο του JSON |

### Επεξεργασία

| Αρχείο | Ρόλος |
|---|---|
| `engine.py` | mapping και επικύρωση, με φραγμένα previews και streamed εξαγωγή |
| `transforms.py` | **μόνο** ρητά transforms· τη σειρά τη δίνει ο καλών |
| `validations.py` | έλεγχοι που διατηρούν την τιμή· η σειρά ανήκει στη μηχανή |
| `conversions.py` | μετατροπή τύπου· καμία επικύρωση, καμία μορφοποίηση διαγνωστικών |

### Είσοδος και έξοδος

| Αρχείο | Ρόλος |
|---|---|
| `sources.py` | context-managed αντάπτορες πηγής (CSV, πίνακας SQL) |
| `exporters.py` | όρια streaming εξόδου σε αρχείο· ποτέ δεν μετασχηματίζει ούτε συμπεραίνει |
| `db_export.py` | γράψιμο σε πίνακα SQL Server: έγκριση, shadow-table swap, batching |
| `discovery.py` | read-only περιήγηση πριν την εκτέλεση — schemas, πίνακες, δείγμα |

### SQL Server (ελεύθερη SQL)

| Αρχείο | Ρόλος |
|---|---|
| `queries.py` | codec ερωτημάτων και **fail-closed** πολιτική ασφαλείας |
| `query_source.py` | μόνο εγκεκριμένο SELECT, bound παράμετροι |
| `query_discovery.py` | authored query datasets, χωριστά από τον κατάλογο πινάκων |

### Ενορχήστρωση και μνήμη

| Αρχείο | Ρόλος |
|---|---|
| `ordered.py` | codec για διαδοχικές ανεξάρτητες εξαγωγές |
| `ordered_runs.py` | σειριακός συντονιστής γύρω από **αμετάβλητη** μηχανή |
| `store.py` | SQLite: ορισμοί, στιγμιότυπα εκτελέσεων, δεσμεύσεις export tables |
| `templates.py` | source-independent blueprints, immutable revisions, ρητό binding, πρόταση πεδίων από πίνακα |
| `binding_profiles.py` | αποθηκευμένα δεσίματα ανά πηγή, ποτέ δεν αποφασίζει μόνο του |
| `diagnostics.py` | παρουσίαση υπαρχόντων στοιχείων· **δεν ξαναδιαβάζει** την πηγή |

### Περίβλημα

| Αρχείο | Ρόλος |
|---|---|
| `web.py` | loopback-only UI για έναν έμπιστο τοπικό χρήστη |
| `config.py` | τοπική εκκίνηση· **ποτέ** log σε connection strings ή περιεχόμενα |
| `__main__.py` | CLI: `serve`, `preview`, `run` |

---

## 9. Το frontend (11 αρχεία, 2.600 γραμμές, χωρίς build step)

```
01 Discover a source     ── πηγή, σύνδεση, περιήγηση καταλόγου
   Reusable mapping templates
   Review mappings       ── εμφανίζεται μόνο μετά από Apply
02 Map & validate        ── μία γραμμή ανά πεδίο εξόδου
      Choose source columns
      Bulk transforms
03 Export                ── αρχείο (CSV/XLSX) ή πίνακας SQL Server
   Advanced              ── το JSON του pipeline
   Results / Run history
   Ordered query exports
```

---

## 10. Η επιφάνεια HTTP

Όλα POST με token, όλα loopback:

`/api/bootstrap` · `/api/columns` · `/api/discovery/…` · `/api/validate` ·
`/api/preview` · `/api/ordered/preview` · `/api/pipelines` ·
`/api/pipelines/delete` · `/api/runs` · `/api/diagnostics/rejections` ·
`/api/query/connections` · `/api/query/validate` · `/api/export/connections` ·
`/api/templates/…` (`list`, `read`, `create`, `revision`, `apply`, `bind`,
`match`, `from_columns`) · `/api/profiles/…` (`list`, `read`, `save`,
`delete`, `reconcile`)

---

## 11. Πραγματικές μετρήσεις ταχύτητας (22/9/2026, ζωντανή SQL Server)

Μετρημένα σε ένα πραγματικό dataset 1.000.000 γραμμών, όχι σε τοπικό CSV μόνο.

| Κατεύθυνση | Ταχύτητα |
|---|---|
| CSV → πίνακας SQL Server (write) | ~4.100–4.400 σειρές/δευτ. |
| Πίνακας SQL Server → CSV (read) | ~9.600 σειρές/δευτ. (~2,2× πιο γρήγορο από write) |

- Το write είναι το πιο ακριβό στάδιο, όχι το read.
- Batch size στο write έχει μεγάλη σημασία: 1.000 σειρές/batch ≈ 2.200/δευτ.,
  5.000 ≈ 4.500/δευτ., 20.000 ≈ 5.900/δευτ. (μετρημένο σε isolation). Ο
  προεπιλεγμένος batch του DB export είναι 20.000.
- `fast_executemany` (pyodbc) δίνει πραγματικό αλλά μέτριο κέρδος όταν όλες
  οι string στήλες έχουν `max_length` (~7-12%). Σε στήλη χωρίς `max_length`
  (NVARCHAR(MAX)) το db_export.py το **απενεργοποιεί αυτόματα** — ο συνδυασμός
  NVARCHAR(MAX) + fast_executemany + μεγάλο batch προκάλεσε πραγματικό
  `MemoryError` σε δοκιμή 6,6 εκατ. γραμμών· διορθώθηκε.
- Εκτίμηση για πλήρη μετάθεση 6,6 εκατ. γραμμών: **~27 λεπτά** — πολύ μέσα στο
  περιθώριο των ωρών που χρειάζεται ένα πραγματικό migration.
- Έλεγχοι εγκυρότητας (required/max_length/type) **δεν** είναι το bottleneck:
  μετρημένη διαφορά ~7% ανάμεσα σε bounded/unbounded στήλη με τους ίδιους
  ακριβώς ελέγχους και στα δύο runs.

---

## 12. Τι λείπει — ειλικρινά, όχι μαρκετίστικα

### Πραγματικά κενά

- **Split export δεν δουλεύει σε ordered (πολλαπλών βημάτων) pipelines.**
  Μπλοκάρεται ρητά με μήνυμα λάθους, όχι σιωπηλά — αλλά η δυνατότητα
  απλά δεν υπάρχει εκεί ακόμα.
- **Ένα saved binding για CSV αναγνωρίζει την πηγή από τη διαδρομή του
  αρχείου.** Αν μετακινήσεις το αρχείο, δεν θα το αναγνωρίσει πια ως
  "ίδια πηγή" — θα το βρεις στη λίστα, απλά πιο κάτω. Δεν χάνεται τίποτα,
  απλά δεν σε βοηθάει αυτόματα.
- **Απλή πηγή πίνακα/view κάνει πάντα `SELECT *`.** Ακόμα κι αν κρατάς 10 από
  173 στήλες, διαβάζονται και οι 173 από τη βάση· μόνο το ελεύθερο SELECT ή
  ένα view περιορίζει τι φεύγει από τον SQL Server πριν φτάσει στην Python.
  Δεν έχει μετρηθεί ακόμα πόσο κοστίζει αυτό σε wide πίνακα, ούτε έχει
  σχεδιαστεί αυτόματο projection με βάση τα mappings.

### Σκόπιμες αποφάσεις που μπορεί να μοιάζουν με κενό αλλά δεν είναι

- **Καμία αυτόματη αντιστοίχιση ονομάτων πέρα από τα προφανή.** Το
  `Match remaining by name` δεν πλησιάζει ποτέ κάτι σαν
  `KOD_PEL → customer_code`. Είναι σκόπιμο: μια λάθος αυτόματη
  αντιστοίχιση περνάει απαρατήρητη και βγάζει λάθος δεδομένα.
- **Δεν υποστηρίζονται stored procedures.** Μόνο SELECT. Οι views
  παραμένουν η γέφυρα για λογική που ήδη υπάρχει στη βάση.
- **Το transform στη SQL δεν προσφέρει σχεδόν τίποτα.** Μετρήσαμε: το να
  σπρώξεις `trim`/`upper` στη βάση αντί στο Python αγοράζει μόνο ~4%. Το
  πραγματικό κέρδος είναι να **περνάνε λιγότερα δεδομένα** συνολικά.

---

## 13. Πρώτη δοκιμή με το demo

1. Άνοιξε την εφαρμογή στο `http://127.0.0.1:8765`, αφού την ξεκινήσεις.
2. Η αρχική φόρμα φορτώνει το demo **Customers · clean export**.
3. Πάτα **Discover a source → Browse datasets** για να δεις τα πεδία του CSV.
4. Πάτα **Preview 100 rows**.
5. Θα δεις **5 εγγραφές: 3 έγκυρες και 2 απορρίψεις** — μία με ID που δεν
   περνά τον αυστηρό έλεγχο ακεραίου και ανύπαρκτη ημερομηνία, μία με κενό
   όνομα ενώ το πεδίο είναι υποχρεωτικό.
6. Πάτα **Save pipeline** για να κρατήσεις τις ρυθμίσεις.
7. Διάλεξε CSV, XLSX ή πίνακα SQL Server (αν έχεις εγκεκριμένη σύνδεση) και
   πάτα **Run & export**.
8. Κατέβασε τα αποτελέσματα από το **Run history**.

---

## 14. Νούμερα, για αναφορά

- **24 αρχεία Python**, 4.042 γραμμές πυρήνα
- **11 αρχεία frontend** (χωρίς build step, καθαρό JavaScript), 2.600 γραμμές
- **397 αυτοματοποιημένα tests** (Python, 25 αρχεία) + **18 σενάρια browser** —
  όλα περνάνε
- **~15 HTTP endpoints**, όλα loopback-only, όλα με token

---

## Άλλα τεκμήρια που παραμένουν, ένα ανά συγκεκριμένο θέμα

- [README.md](README.md) — εγκατάσταση και πρώτη εκτέλεση
- [TEMPLATES_EXPLAINED_EL.md](TEMPLATES_EXPLAINED_EL.md) — templates και saved
  bindings, με πλήρες τρέχον παράδειγμα
- [REUSABLE_MAPPING_TEMPLATES.md](REUSABLE_MAPPING_TEMPLATES.md) — η τεχνική
  προδιαγραφή των templates
- [DEVELOPER_NOTE_DATA_FLOW.md](DEVELOPER_NOTE_DATA_FLOW.md) — η ροή
  δεδομένων για προγραμματιστή (SourceRow → RowResult → Exporter)
- [READ_ONLY_QUERY_SOURCES.md](READ_ONLY_QUERY_SOURCES.md) — η πολιτική
  ασφαλείας των ελεύθερων ερωτημάτων
- [ORDERED_QUERY_PIPELINES.md](ORDERED_QUERY_PIPELINES.md) — οι διαδοχικές
  εξαγωγές πολλαπλών βημάτων
- [PROCESSING_DIAGNOSTICS.md](PROCESSING_DIAGNOSTICS.md) — ο viewer των
  απορρίψεων
- [GUIDED_SOURCE_DISCOVERY.md](GUIDED_SOURCE_DISCOVERY.md) — η περιήγηση πηγών
